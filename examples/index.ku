import "std.http"
fn main() {
    app = http.service
    print(app)
    try {
        app.get('/idnex',((res,req)=>{
            print(`{res}`)
        }))
    } catch (err) {
        print(err)
    }
}