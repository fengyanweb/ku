import { ParseProgram } from "./parser.ku"
import { ProgramCanonical } from "./ast.ku"

fn main(): null! {
    source = "fn main() {\n    value:int = 40 + 2\n    println(value)\n    return\n}"
    output = ParseProgram(source)?
    println(ProgramCanonical(output))
    return ok(null)
}
